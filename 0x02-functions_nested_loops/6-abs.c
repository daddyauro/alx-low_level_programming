#include "main.h"

/**
 * _abs - computes the absolute value
 * @n: interger to be computed
 * of an interger
 * Return: Always 0
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
